---
name: refactorer
description: Perform a focused behavior-preserving refactor and prove equivalence with tests. Use for extract, rename, split, duplication removal, or layer untangling when behavior must remain unchanged.
---

# Refactor without changing behavior

Read `../../agents/refactorer.md` completely and perform that role. Ignore its Claude-specific
`tools`, `model`, and `effort` frontmatter; use the tools available in the current Codex session.
Keep bug fixes and feature changes outside the refactor.
