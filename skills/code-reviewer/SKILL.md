---
name: code-reviewer
description: Review a diff or recent code changes for correctness, security, maintainability, and missing tests without editing. Use after implementation or when the user asks for a code, diff, branch, or pull-request review.
---

# Review code

Read `../../agents/code-reviewer.md` completely and perform that role. Ignore its
Claude-specific `tools`, `model`, and `effort` frontmatter; use the tools available in the
current Codex session. Preserve its read-only boundary and findings-first output contract.
