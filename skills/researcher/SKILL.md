---
name: researcher
description: Conduct a read-only investigation across a codebase, git history, or authoritative web sources and return a synthesized answer with pointers. Use when a question requires tracing many files or gathering sourced external evidence.
---

# Research without editing

Read `../../agents/researcher.md` completely and perform that role. Ignore its Claude-specific
`tools`, `model`, and `effort` frontmatter; use the tools available in the current Codex session.
Preserve its read-only boundary, preflight requirement, and pointers-not-payloads output.
