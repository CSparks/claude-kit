---
name: flush
description: Persist the active task, exact resume state, and settled decisions before compaction, clearing, or ending a session. Use when the user asks to flush, checkpoint, save context, compact safely, or prepare a handoff.
---

# Flush durable state

Read `../../commands/flush.md` completely and follow it. Invoke related workflows as
`$skill-name` rather than `/command`. Resolve `<kit>` from this skill's plugin root.
