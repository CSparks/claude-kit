---
id: KIT-D047
title: Dispatch-ladder enforcement shape (KIT-T151, Chris 2026-07-23): (1) kit work agents (researcher/code-reviewer/refactorer/test-author) pin model: opus in frontmatter — a delegation never inherits the session model; (2) new dispatch-guard.mjs PreToolUse(Task|Agent) hook blocks the SILENT fable inherit (no model on call + no frontmatter pin + fable session transcript); (3) explicit model:'fable' stays legal — it is a chosen tier and dispatch.tiers deep expands to it; escape for a deliberate model-less inherit = inline [allow-fable: <reason>]. Rationale: fable is expensive; ladder compliance was memory-dependent (lived failure: 5 researcher agents inherited fable in groovegrid). Recorded at the kit source per the dated-model-judgment rule.
date: 2026-08-02
supersedes:        # DEC-### this replaces, or blank
source:            # commit hash / doc path / "conversation YYYY-MM-DD"
---

**Decision:** <what was decided>

**Why:** <the reason — and what was rejected, and why>

<!-- One decision per file (atomic, like a ticket — KIT-D009). IDs KIT-D### (e.g. KIT-D010),
     assigned in order, never reused. Allocate with next-id.mjs (KIT-T009). Append a NEW
     file to supersede an old one; never edit a settled decision's substance. The orient hook
     surfaces recent decisions each session. Cite the id in commits where relevant. -->
