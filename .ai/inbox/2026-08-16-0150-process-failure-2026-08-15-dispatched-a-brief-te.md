(bug) Process failure (2026-08-15): dispatched a brief telling the agent to FILE a new ticket for the scripts/test-hooks.mjs split without running q fts first — KIT-T212 already ticketed it; agent created duplicate KIT-T239. ROOT CAUSE: orchestrator wrote 'file the ticket' into a brief instead of grounding in the store before dispatch (the pre-filing dedup rule the doctrine batch just added to CLAUDE.global.md for direct filings must also cover briefs that instruct filing). Fix direction: the drain/work brief template forbids 'create a ticket' instructions unless the id is pre-allocated by the orchestrator after a q fts check.
