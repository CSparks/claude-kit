(feature) Framework-store pattern: a shared-dependency repo (rapid-game, a git submodule consumed by several games) needs its OWN .ai store (RG key) registered in claude-kit-projects.json, with (a) 't move' across stores leaving a pointer stub in the source store, (b) a 'q conflicts <topic>' cross-scope query that surfaces decisions in different scopes ruling on the same nouns, (c) a commit-gate rule that a decision naming a framework crate (rg-*) filed in a GAME store warns 'belongs in RG'. Root cause seen 2026-08-29 (stiletto ST-D117): 47 ST + 28 HOD + 2 MAR framework decisions scattered across game stores; rapid-game has no store; Chris: 'We can't be making conflicting decisions.'
