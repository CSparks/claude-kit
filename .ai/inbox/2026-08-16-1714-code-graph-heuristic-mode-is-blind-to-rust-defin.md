(bug) code-graph heuristic mode is blind to Rust: 'defines spawn_bumper' misses 'pub(super) fn spawn_bumper' in marblequest/world/bumper.rs (only finds a plain 'pub fn' twin in marble-madness-core), and importers-of / surface on marblequest paths return [] because 'use crate::x::y' / 'mod x' edges are not resolved. Repo IS fully indexed (156/165 files) — supersedes my two 'wrong crate / not indexed' captures from 17:12, which were misdiagnoses. Fix: match pub(crate)/pub(super) visibility and resolve Rust module paths.
