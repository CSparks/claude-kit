(feature) An editor-workbench specialist agent has earned itself

The rapid-game HTML editor domain (React/vite editor, @game-manifest injection,
/api/editor/* file-bridge contract, per-game vite config extension) has now recurred
across HOD (HOD-T160/T115) and stiletto (ST-T127). Tonight it got another long
hand-written brief to general-purpose — per the dispatch contract that is a
specialist that was never written down. Scaffold `agents/editor-workbench.md` in the
kit (model pinned opus, conventions: manifest injection pattern, contract pointers,
the vite mergeConfig proxy-override gotcha, no-screenshot evidence rules).
