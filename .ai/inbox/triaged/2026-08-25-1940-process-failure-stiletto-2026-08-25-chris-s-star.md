(bug) Process failure (stiletto 2026-08-25): Chris's starting-MMP spec (docs/DESIGN.md Modular Vehicles, 08-22) was on the record and the rig asset never followed it — 'I described what I wanted in a previous session and you've ignored all that'. Root cause: asset tickets are written from the code's current shape, not grounded against the design doc; the orient/work step needs a 'design-doc grounding' check for any asset the DESIGN.md names.
