(feature) Web UI/API: tick acceptance criteria and create items from the browser - Chris cannot check off criteria (e.g. GG-T089 EIN, bank account) or add new items without the CLI. t.mjs already exposes tick() and scaffoldNew(); the API has no endpoint and AcceptanceList.tsx renders disabled checkboxes by design (KIT-D032 said criteria are CLI-only). Revisit that for the maintainer path: POST criteria tick + POST new ticket, wired through the same t.mjs guards as comment/status writes.
