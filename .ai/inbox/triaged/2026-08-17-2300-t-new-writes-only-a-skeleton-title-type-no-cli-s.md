(bug) t new writes only a skeleton (title+type): no CLI surface for description, acceptance criteria, priority, parent, labels, files — filing ST-T296..298 needed a hand-written Node script to fill the bodies (2026-08-17, stiletto). t new should accept --desc/--ac (repeatable)/--priority/--parent/--labels/--files (or read a body from stdin/--from <file>) so a filing is one command. Related: query-gate blocked a heredoc cat > .ai/decisions/… and a sed -n on a ticket file — WRITE/READ of one named .ai file is not a store grep; the gate should distinguish a single named path from a search.
