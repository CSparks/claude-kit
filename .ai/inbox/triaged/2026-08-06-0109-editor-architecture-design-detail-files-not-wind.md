(bug) Editor-architecture design detail (files-not-windows hosting, never embed the game, per-game manifest) lived only in an out-of-repo session's chat + partially in a submodule plan doc: the consuming project's decisions store got nothing, q indexes neither submodule docs/ nor other projects' research, and the home-dir session had no .ai to force capture. Next session re-derived the direction wrongly from another project's tickets. Root cause: decisions made in out-of-repo sessions are not routed to the affected project's decision store; plan-of-record docs cited by tickets are invisible to fts. Candidate fixes: cap/route decisions to the affected project even when cwd is not a repo; q indexes submodule docs/; orient surfaces plan-doc paths cited by open tickets
