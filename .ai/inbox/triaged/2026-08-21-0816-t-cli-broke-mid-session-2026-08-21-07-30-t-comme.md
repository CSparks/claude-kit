(bug) t CLI broke mid-session (2026-08-21 ~07:30): 't comment ST-T347' returns "unknown id 'ST-T347' — no ticket file carries it" though .ai/tickets/ST-T347-*.md exists with id: ST-T347 frontmatter and t comment/status on the SAME id succeeded earlier tonight (comments #1/#2, status todo→doing). 't new' concurrently degraded: prints 'pad: 3' and creates NO file (two silent creation failures). q verify says ST fresh; other ids (ST-T348, ST-T324) still work. Repro was consecutive t calls in one session incl. t tick with out-of-range ordinal on ST-T348. Root cause unknown — needs the t id-resolution path audited; silent no-op creation is the worse half.
