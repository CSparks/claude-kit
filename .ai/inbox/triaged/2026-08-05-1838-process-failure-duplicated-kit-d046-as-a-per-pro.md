(bug) PROCESS FAILURE: duplicated KIT-D046 as a per-project decision (jollys-vinyl JV-D006, 2026-08-05). The account-of-record fact was ALREADY in the durable record at kit level since 2026-08-02; I wrote a second copy of it scoped to one project, which is the anti-pattern the base contract names (cross-project knowledge must be portable via claude-kit, never a per-project memory/decision). ROOT CAUSE: I grounded on the NEGATIVE only — searched the JV store for 'depixeled-chris' and found the stale JV-Q003, then wrote a new decision without ever querying for the POSITIVE fact ('account of record', 'CSparks') across stores, where KIT-D046 would have surfaced immediately. q fts is cross-store; I ran it scoped to one project's symptom. WANTED: (a) triage/dedup guard — before a decision file is created, fts its title terms across ALL stores and surface near-duplicates (a decision whose title matches an existing decision at another scope is almost always a mis-scoped duplicate); (b) consider making it a pre-write gate on .ai/decisions/*.md, matching the existing gate style, since this is exactly the 'ground before you propose' rule that currently depends on memory.
