(bug) PROCESS FAILURE (gridiron-blitz 2026-08-17): a night of UI screens shipped 'green' yet read as slap-dash / tiny on 4k / pad-hostile — root cause: UI acceptance was 'layout fits inside the window' at one or two small sizes, no design spec gate, no fills-fraction assert, no 4k in the size matrix, no pad walkthrough test. Kit needs a UI-screen ticket template / gate: spec-before-build criterion auto-added to ui-labelled tickets, fit matrix must include 4k, and a 'fills >= fraction' + PadNav walkthrough criterion (see GB-D028).
