(feature) Agents must DETECT and FLAG concurrent tree activity, not just be told 'sole owner': cargo 'Blocking waiting for file lock on build directory' waits, unexpected dirty/changed files they didn't write, foreign commits landing on the branch mid-task - each should surface a loud flag in the report (and optionally pause). Also: orient should surface OTHER live sessions' recent commits on the current repo. Trigger: Chris 2026-08-06, two sessions were live in stiletto-2349 the same afternoon (T138 split + T145 merge from one; T148/T057 agents from the other) with no cross-flagging; cargo target/ lock contention between them is plausible and invisible. Existing kit coverage is doctrine only (one-tree-one-agent rule, branch-guard KIT-T082) - nothing detects the OTHER session.
