(bug) kit bin/ shims (code-graph, cap, t, q, rem) were on NEITHER the process nor the user PATH on the Windows machine — code-graph unusable in-session until invoked by full path. Root cause: bootstrap composes CLAUDE.md but never provisions the Windows user PATH with claude-kit\bin (bootstrap.sh is bash; no Windows PATH step exists). Immediate fix applied 2026-08-05: D:\dev\claude-kit\bin appended to the user PATH on this machine (new shells only). Kit fix needed: bootstrap (or a setup.ps1 counterpart) must own the PATH provisioning so the tools are ready on every machine. Also: cap has no --help handling — 'cap --help' captured the literal text as an inbox item; an args parse guard is warranted.
