(bug) claude-kit has NO framework-scoped context layer: rules binding every rapid-game consumer cannot surface in those projects. ST-D042 (every asset dial must appear in the editor; assets built from rg-meshkit complex shapes, never jammed primitives) is UNIVERSAL across rapid-game games, but a decision file is PASSIVE — nothing queries it before work starts. Project CLAUDE.md is too narrow (hand-copy into every game = the same drift defect), the global base too wide (fires in non-rapid-game repos). Need a layer that loads automatically in exactly the projects using a given framework/submodule, plus a gate that fires before asset work. Root cause: durable cross-project rules have only two auto-loading homes (global CLAUDE.md, hooks) and neither is framework-scoped.
