(bug) t comment: when --author is omitted the author defaults to the machine user (KIT_USER/registry 'chris'), so agent-written evidence comments land in tickets signed '@chris' (seen twice 2026-08-29: ST-T456#1, ST-T459#1 — corrected by follow-up comments). Authorship is a durable claim; an agent must never be attributed to the maintainer. Fix: require --author explicitly (error without it), or default to 'claude' when CLAUDE_AGENT/CLAUDECODE env is present.
