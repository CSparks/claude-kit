(bug) codex 0.147.0 rejects the kit's plugin hooks config: "failed to parse plugin hooks config C:\Users\Chris Sparks\.codex\plugins\cache\claude-kit\claude-kit\0.1.16\hooks\hooks.json: unknown field `$schema`, expected `description` or `hooks`". Kit's hooks.json carries a top-level `$schema` key codex's parser no longer tolerates, so kit hooks silently don't load in codex sessions. Fix at kit source: drop or relocate the `$schema` key from hooks/hooks.json (verify codex still accepts `description`/`hooks`-only shape). Observed 2026-08-21 while fixing codex-local wire_api breakage on the Windows box.
